// Detection engine (pure, unit-tested). Implemented in Phase 2 — see BUILD_PLAN.md §3.
// Signals: burstiness, duplication, aiGenerated, generic, ratingAnomaly, sentimentMismatch, incentivized.
export {};
